// src/UserContext.js

import React from 'react';

// Creating a UserContext to manage the user data across components
const UserContext = React.createContext();

// Exporting the context for use in other components
export default UserContext;
